import React from 'react';

function Header(props) {
    return (
        <div className="Header">
            <button>Start</button>
            <span></span>
        </div>
    )
}

export default Header;
